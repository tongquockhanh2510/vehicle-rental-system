Mô tả vấn đề api-gateway bị lỗi
POST http://localhost:8000/api/users/login gọi api này hơn 2 phút vẫn không có phản hồi
POST http://localhost:3001/api/users/login vẫn hoạt động bình thường
Sửa lại api-gateway để có thể hoạt động được